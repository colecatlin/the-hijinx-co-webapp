import React from 'react';
import IdentityReviewDashboard from '@/components/racecore/identity/IdentityReviewDashboard';

export default function IdentityReviewPage() {
  return <IdentityReviewDashboard />;
}